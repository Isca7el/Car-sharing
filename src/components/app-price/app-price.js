const AppPrice = () => {
  // const currency = ' €';
  // const price = null;
  // return `${price + currency}`;\
  // eslint-disable-next-line prettier/prettier
  return <h4> Цена машины</h4>
};

export default AppPrice;
